public class Enemy : Character
{
    public Enemy(string _name, int _hitPoints, int damage) : base(_name, _hitPoints, damage) { }

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
